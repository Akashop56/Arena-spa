<?xml version="1.0" encoding="utf-8"?>
<!--
  RONIN stores encrypted API keys and personal memories on device. The Keystore
  master key can never leave the device, so a backup would only ever restore
  undecryptable blobs. Everything is therefore excluded from cloud backup and
  device-to-device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
